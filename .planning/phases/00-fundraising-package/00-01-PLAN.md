---
phase: 00-fundraising-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tailwind.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/api/waitlist/route.ts
  - src/app/robots.ts
  - src/app/sitemap.ts
  - src/components/sections/Hero.tsx
  - src/components/sections/Problem.tsx
  - src/components/sections/Solution.tsx
  - src/components/sections/HowItWorks.tsx
  - src/components/sections/ForFunders.tsx
  - src/components/sections/Footer.tsx
  - src/components/WaitlistForm.tsx
  - src/lib/kit.ts
  - public/og-image.png
  - .env.example
  - .env.local
  - .gitignore
  - tsconfig.json
autonomous: false
requirements: []
user_setup:
  - service: kit
    why: "Email waitlist capture (free up to 10K subscribers)"
    env_vars:
      - name: KIT_API_KEY
        source: "Kit (ConvertKit) Dashboard -> Settings -> API -> API Key"
      - name: KIT_FORM_ID
        source: "Kit Dashboard -> Forms -> Create a new form -> The numeric ID in the form URL"
    dashboard_config:
      - task: "Create a Kit account at kit.com (free plan)"
        location: "https://kit.com"
      - task: "Create a form called 'TaV Waitlist' to capture email signups"
        location: "Kit Dashboard -> Forms -> New Form"
  - service: vercel
    why: "Free hosting for Next.js site"
    env_vars: []
    dashboard_config:
      - task: "Create a Vercel account and connect the GitHub repo paxtontribe/TakesaVillage"
        location: "https://vercel.com/new"
      - task: "Add custom domain in Vercel project settings after domain is purchased"
        location: "Vercel Dashboard -> Project -> Settings -> Domains"
  - service: domain
    why: "Custom domain for the website"
    env_vars: []
    dashboard_config:
      - task: "Register takesavillage.health (or takesavillage.care) via Cloudflare Registrar or Namecheap. Cost: ~$70-100/year for .health, ~$15-30/year for .care"
        location: "https://dash.cloudflare.com or https://www.namecheap.com"

must_haves:
  truths:
    - "A visitor landing on the site immediately understands what Takes a Village is and can sign up for the waitlist"
    - "The For Funders/Partners section communicates technical depth and links to the GitHub planning docs"
    - "The waitlist form captures email addresses and sends them to Kit"
    - "The site is responsive and works on mobile devices"
    - "The site has proper SEO metadata and Open Graph tags for social sharing"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page composing all sections"
      contains: "Hero"
    - path: "src/app/api/waitlist/route.ts"
      provides: "API route that POSTs to Kit"
      exports: ["POST"]
    - path: "src/components/WaitlistForm.tsx"
      provides: "Email capture form component"
      contains: "email"
    - path: "src/components/sections/ForFunders.tsx"
      provides: "Funder-facing section with GitHub link"
      contains: "github.com/paxtontribe/TakesaVillage"
  key_links:
    - from: "src/components/WaitlistForm.tsx"
      to: "/api/waitlist"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/waitlist"
    - from: "src/app/api/waitlist/route.ts"
      to: "Kit API"
      via: "POST to Kit forms endpoint"
      pattern: "api.convertkit.com|api.kit.com"
---

<objective>
Build the Takes a Village landing page website with Next.js 15 on Vercel free tier.

Purpose: Create a professional, warm, and compelling website that communicates the TaV mission, captures waitlist signups via Kit (ConvertKit), and provides a dedicated section for funders and partners. This is the centerpiece of the Phase 0 fundraising package — if a funder lands on it cold, they should understand what TaV is and want to learn more.

Output: A deployable Next.js 15 app with brand identity (colors, typography), all six content sections (Hero, Problem, Solution, How It Works, For Funders, Footer), working email waitlist capture via Kit API, responsive design, and SEO metadata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-fundraising-package/00-CONTEXT.md
@.planning/phases/00-fundraising-package/00-RESEARCH.md

<interfaces>
<!-- No existing code interfaces — this is a greenfield project. -->
<!-- Brand identity and component architecture are defined below in task actions. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project with brand identity and all page sections</name>
  <files>
    package.json, next.config.ts, tailwind.config.ts, tsconfig.json, .gitignore, .env.example,
    src/app/layout.tsx, src/app/page.tsx, src/app/robots.ts, src/app/sitemap.ts,
    src/app/globals.css,
    src/components/sections/Hero.tsx, src/components/sections/Problem.tsx,
    src/components/sections/Solution.tsx, src/components/sections/HowItWorks.tsx,
    src/components/sections/ForFunders.tsx, src/components/sections/Footer.tsx,
    src/components/WaitlistForm.tsx, src/lib/kit.ts,
    src/app/api/waitlist/route.ts, public/og-image.png
  </files>
  <read_first>
    .planning/phases/00-fundraising-package/00-CONTEXT.md
    .planning/phases/00-fundraising-package/00-RESEARCH.md
    .planning/PROJECT.md
    .planning/research/SUMMARY.md
    .planning/research/FEATURES.md
    .planning/research/PITFALLS.md
  </read_first>
  <action>
    1. Scaffold the project:
       ```
       npx create-next-app@latest . --typescript --tailwind --app --src-dir --no-import-alias
       npx shadcn@latest init
       npx shadcn@latest add button input card badge
       npm install motion
       ```

    2. Brand identity — configure in tailwind.config.ts and src/app/globals.css:
       - Primary color: teal #2D7D6F (trust, health) — map to CSS variable --primary
       - Accent color: warm amber #F4A261 (hope, action) — map to CSS variable --accent
       - Background: near-white #F8F9FA — map to CSS variable --background
       - Text: near-black navy #1A1A2E — map to CSS variable --foreground
       - Typography: Inter font (system default for shadcn/ui, loaded via next/font/google)
       - Per D-06: clean, professional design using Tailwind + shadcn/ui defaults
       - Per D-02: warm + empowering tone throughout all copy

    3. Root layout (src/app/layout.tsx):
       - Import Inter from next/font/google
       - SEO metadata object with title "Takes a Village — Verified Substance Abuse Treatment Transparency", description "A nonprofit platform making substance abuse treatment radically transparent. No pay-to-rank. No hidden agendas. Real reviews from real people.", openGraph with title/description/image/url (use https://takesavillage.health as placeholder — update after domain purchase), twitter card "summary_large_image"

    4. Main page (src/app/page.tsx):
       - Import and compose all section components in order per D-07: Hero, Problem, Solution, HowItWorks, ForFunders, Footer
       - Each section wrapped in a motion scroll-reveal using LazyMotion + domAnimation from "motion/react":
         ```tsx
         <m.section initial={{ opacity: 0, y: 24 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 0.5 }} viewport={{ once: true }}>
         ```

    5. WaitlistForm component (src/components/WaitlistForm.tsx):
       - Single email input field (per D-04: email only, lowest friction)
       - Submit button with text "Join the Waitlist"
       - On submit: POST to /api/waitlist with { email }
       - Show success state: "You're on the list!" with a checkmark
       - Show error state if API returns error
       - Uses shadcn/ui Input and Button components

    6. Kit API integration:
       - src/lib/kit.ts: export async function subscribeToWaitlist(email: string) that POSTs to `https://api.convertkit.com/v3/forms/${process.env.KIT_FORM_ID}/subscribe` with body { api_key: process.env.KIT_API_KEY, email }
       - src/app/api/waitlist/route.ts: export async function POST(request) that reads { email } from request body, calls subscribeToWaitlist, returns 200 on success or 500 on error. Include basic email format validation.
       - .env.example containing KIT_API_KEY=your_kit_api_key_here and KIT_FORM_ID=your_kit_form_id_here

    7. Hero section (src/components/sections/Hero.tsx):
       - Large heading: "You Deserve Better Information About Treatment"
       - Subheading: "Takes a Village is a nonprofit platform making substance abuse treatment radically transparent. No pay-to-rank. No hidden agendas."
       - WaitlistForm component embedded directly
       - Optional: waitlist counter placeholder text "Join X others waiting for better information" (hardcoded to start, can be made dynamic later via Kit API subscriber count)
       - Statement: "Nonprofit — no facility pays to rank"

    8. Problem section (src/components/sections/Problem.tsx):
       - Heading: "The System Is Broken"
       - Key statistics from research corpus (use these exact numbers):
         * "$42 billion US substance abuse treatment industry"
         * "~17,000 treatment facilities with no verified review system"
         * "FTC documented predatory patient brokering practices"
         * "SAMHSA's locator lacks reviews, cost data, and outcome tracking"
       - Use shadcn/ui Card components for each stat
       - Tone per D-02: factual and empathetic, not alarmist

    9. Solution section (src/components/sections/Solution.tsx):
       - Heading: "What Takes a Village Does Differently"
       - Four differentiators displayed as cards or icon+text blocks:
         * "Verified Reviews" — authenticated-anonymous reviews from real patients, verified attendance
         * "Radical Transparency" — every facility gets a transparency score based on verification, outcomes, and community feedback
         * "Decentralized Data" — facility data lives on a knowledge graph no single party controls
         * "AI-Powered Discovery" — describe your situation in plain language and get matched to treatment options
       - Each card: short title + 1-2 sentence description
       - No blockchain jargon — describe what it does for the user, not how it works technically

    10. HowItWorks section (src/components/sections/HowItWorks.tsx):
        - Heading: "How It Works"
        - Three-step visual flow for non-technical audience:
          * Step 1: "Search" — find facilities by location, insurance, treatment type
          * Step 2: "Verify" — see transparency scores, verified reviews, and real outcomes
          * Step 3: "Connect" — find funding options and apply directly through the platform
        - Simple numbered steps with icons or illustrations (use emoji or shadcn Badge as placeholder icons)

    11. ForFunders section (src/components/sections/ForFunders.tsx) — per D-03:
        - Heading: "For Funders and Partners"
        - Brief paragraphs covering:
          * The problem scope ($42B industry, 17K facilities, documented predatory practices)
          * The technical approach (decentralized knowledge graph, verified identity, AI agents) — explained simply
          * Legal awareness: "We've identified and mapped three critical legal compliance gates — Anti-Kickback Statute, 42 CFR Part 2 healthcare privacy, and FinCEN money transmission — before writing a line of platform code."
          * The ask: "We're seeking grant funding and early partners to build the technical platform."
        - Link to GitHub: "View our full technical architecture, legal analysis, and roadmap" linking to https://github.com/paxtontribe/TakesaVillage/tree/main/.planning
        - Contact CTA: WaitlistForm embedded again with framing "Get in touch — join our waitlist or email us directly"

    12. Footer section (src/components/sections/Footer.tsx):
        - Social media links (placeholder URLs — will be updated in Plan 00-02): Instagram, TikTok, X, LinkedIn
        - GitHub link: https://github.com/paxtontribe/TakesaVillage
        - SAMHSA National Helpline: "If you or someone you know needs help now: 1-800-662-4357 (SAMHSA National Helpline)"
        - "Takes a Village is a nonprofit project." copyright line
        - WaitlistForm one more time (third placement)

    13. SEO files:
        - src/app/robots.ts: allow all crawlers, reference sitemap at https://takesavillage.health/sitemap.xml
        - src/app/sitemap.ts: export default sitemap with single entry for "/" with lastModified as current date

    14. Create a simple og-image.png placeholder in public/ — a solid teal (#2D7D6F) rectangle with white text "Takes a Village" centered. Use a simple SVG-to-PNG approach or just create a placeholder that will be replaced with a Canva-designed image later.
  </action>
  <verify>
    <automated>cd /Users/celenapaxton/Desktop/TakesaVillage && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next" and "motion" as dependencies
    - src/app/layout.tsx contains "Takes a Village" in metadata title
    - src/app/layout.tsx contains "openGraph" configuration
    - src/app/page.tsx imports Hero, Problem, Solution, HowItWorks, ForFunders, Footer
    - src/components/WaitlistForm.tsx contains "api/waitlist" fetch call
    - src/app/api/waitlist/route.ts contains "export async function POST"
    - src/app/api/waitlist/route.ts contains "api.convertkit.com" or "api.kit.com"
    - src/lib/kit.ts contains "subscribeToWaitlist" export
    - src/components/sections/Hero.tsx contains "You Deserve Better Information"
    - src/components/sections/Problem.tsx contains "$42 billion" and "17,000"
    - src/components/sections/Solution.tsx contains "Verified Reviews" and "Radical Transparency"
    - src/components/sections/HowItWorks.tsx contains "Search" and "Verify" and "Connect"
    - src/components/sections/ForFunders.tsx contains "github.com/paxtontribe/TakesaVillage"
    - src/components/sections/ForFunders.tsx contains "Anti-Kickback" and "42 CFR Part 2" and "FinCEN"
    - src/components/sections/Footer.tsx contains "1-800-662-4357"
    - .env.example contains "KIT_API_KEY" and "KIT_FORM_ID"
    - src/app/robots.ts exists
    - src/app/sitemap.ts exists
    - tailwind.config.ts contains "#2D7D6F" or "2D7D6F" (teal primary color)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    The Next.js project builds successfully with all six sections rendering, brand colors applied, waitlist form wired to Kit API route, SEO metadata present, and responsive layout working. The site communicates the TaV mission clearly to both general visitors and funders.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify website locally and deploy to Vercel</name>
  <files>src/app/page.tsx</files>
  <action>
    This is a human verification checkpoint. The executor should:
    1. Start the dev server with `npm run dev`
    2. Present the URL http://localhost:3000 to the user for visual review
    3. Walk through the verification steps listed in how-to-verify below
    4. Wait for user approval before marking this plan as complete
  </action>
  <what-built>
    Complete Takes a Village landing page with all six sections (Hero, Problem, Solution, How It Works, For Funders, Footer), email waitlist capture via Kit API, responsive design, and SEO metadata. Brand identity: teal primary (#2D7D6F), amber accent (#F4A261), Inter typography.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` in the project root
    2. Visit http://localhost:3000 in your browser
    3. Check the following:
       - Hero section shows mission statement and waitlist signup form
       - Problem section shows four statistics about the broken treatment system
       - Solution section shows four differentiators (Verified Reviews, Radical Transparency, Decentralized Data, AI Discovery)
       - How It Works section shows three steps (Search, Verify, Connect)
       - For Funders section mentions legal compliance gates and links to GitHub
       - Footer shows social media links and SAMHSA helpline number
       - Waitlist form appears in Hero, For Funders, and Footer
    4. Test on mobile viewport (resize browser to ~375px width) — all sections should stack and remain readable
    5. If Kit API key is configured in .env.local, test the waitlist form by entering an email
    6. Before proceeding: Ensure domain is purchased and Vercel is connected to GitHub repo
  </how-to-verify>
  <verify>
    <automated>cd /Users/celenapaxton/Desktop/TakesaVillage && npm run build</automated>
  </verify>
  <done>User has visually verified the website looks correct and approved it for deployment.</done>
  <resume-signal>Type "approved" to proceed, or describe any visual/content/layout issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npm run lint` passes
- All six sections render on the page
- Waitlist form POSTs to /api/waitlist endpoint
- SEO metadata present in page source (check with View Source or curl)
- Site is responsive (mobile viewport)
- Brand colors (teal #2D7D6F, amber #F4A261) are applied
</verification>

<success_criteria>
A complete, deployable Next.js 15 landing page exists in the repo with brand identity, all content sections per D-07, working Kit email capture, responsive layout, and SEO metadata. The site communicates the TaV mission to cold visitors and provides funders with evidence of technical depth and legal diligence.
</success_criteria>

<output>
After completion, create `.planning/phases/00-fundraising-package/00-01-SUMMARY.md`
</output>
